[project]
name = "weather-forecasting-project"
version = "0.1.0"
description = "Weather forecasting data fetcher using TMD API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas==2.2.2",
    "python-dotenv==1.0.1",
    "requests==2.32.3",
]

[tool.uv]
package = false
